import { injectable } from "inversify";
import { {{> processorInterfaceName}} } from "@infrastructure/interfaces/{{> processorPath}}";

@injectable()
export class {{> processorName}} implements {{> processorInterfaceName}} {
  constructor() {}
}
